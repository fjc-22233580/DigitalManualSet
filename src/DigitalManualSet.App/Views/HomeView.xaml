<UserControl
    x:Class="DigitalManualSet.App.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:DigitalManualSet.App.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:HomeViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>

        <Style
            x:Key="HomeTileStyle"
            BasedOn="{StaticResource DefaultButtonStyle}"
            TargetType="{x:Type Button}">
            <Setter Property="Height" Value="75" />
            <Setter Property="Width" Value="175" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>


        <Button
            Grid.Row="0"
            Grid.Column="0"
            Command="{Binding CreatePackageCommand}"
            Content="Create package"
            Style="{StaticResource HomeTileStyle}" />
        <Button
            Grid.Row="0"
            Grid.Column="1"
            Command="{Binding OpenPackageCommand}"
            Content="Open previous package"
            Style="{StaticResource HomeTileStyle}" />
        <Button
            Grid.Row="1"
            Grid.Column="0"
            Content="Different Button"
            Style="{StaticResource HomeTileStyle}" />
        <Button
            Grid.Row="1"
            Grid.Column="1"
            Command="{Binding SettingsCommand}"
            Content="Settings"
            Style="{StaticResource HomeTileStyle}" />

    </Grid>
</UserControl>
